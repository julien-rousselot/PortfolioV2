---
type Props = {
  tag: string;
};

const { tag } = Astro.props;
---

<a
  href={`/tags/${encodeURI(tag)}`}
  target="_self"
  class="px-2.5 py-1 rounded-lg text-xs text-orange-400 font-medium bg-white capitalize"
>
  {tag}
</a>
